# Exclude syncthing enablement symlinks (absolute targets; GNU Stow rejects them).
# These are generated by `systemctl --user enable syncthing.service` and are
# recreated by stow_configs.sh, so the package must not carry them.
\.target\.wants/syncthing\.service$
