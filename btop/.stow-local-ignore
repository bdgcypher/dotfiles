# Pywal cache symlink — created as absolute symlink in post-stow hook
# because stow's directory symlinks change physical path depth,
# breaking relative symlink resolution.
\.config/btop/themes/current\.theme
