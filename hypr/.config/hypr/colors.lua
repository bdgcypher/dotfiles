-- Fallback color variables.
-- Overridden at load time by the pywal-generated colors (sourced after this file).
-- NOTE: intentionally a global (no `local`) so other modules can reference it,
-- matching the hyprlang `$border-active` semantics.
border_active = "rgb(444444)"
