-- Default programs (referenced by the bindings).
-- NOTE: intentionally globals (no `local`) so the bindings modules can use them,
-- matching the hyprlang `$terminal`/`$fileManager`/`$browser` semantics.
terminal    = "ghostty"
fileManager = "nautilus"
browser     = "firefox"
